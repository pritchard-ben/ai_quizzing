import { SafeAreaView, StyleSheet, Text, View } from "react-native";
import React from "react";
import questions from "./HomeScreen"

const QuizScreen = () => {
  return (
    <SafeAreaView>
      <Text>QuizScreen</Text>
    </SafeAreaView>
  );
};

export default QuizScreen;

const styles = StyleSheet.create({});
